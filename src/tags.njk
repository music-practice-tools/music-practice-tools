---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - activity
    - activitys
    - tune
    - tunes
    - tagList
  addAllPagesToCollections: true
layout: home.html.njk
renderData:
  title: Tagged “{{ tag }}”
permalink: /tags/{{ tag }}/
---
<h1>Tagged “{{ tag }}”</h1>

{% set activityslist = collections[ tag ] %}
{% include "activityslist.html.njk" %}

<p>See <a href="{{ '/tags/' | url }}">all tags</a>.</p>
